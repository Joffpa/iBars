
interface JQueryStatic {
    formatNumber(settings: any);
    formatNumber(num: string, settings: any);
    
    parseNumber(settings: any, writeBack: boolean, giveReturnValue: boolean);
    parseNumber(num: string, settings: any);
}